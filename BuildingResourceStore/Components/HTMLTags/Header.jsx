module.exports =  {
    class : React.createClass({ // 고정

      // ReactJS 개발 가이드에 따라 컴포넌트를 구현합니다.

      // 예시
      render: function () {
        return (
          <header style={{padding:'10px', outline:'1px solid #333'}}>
            header
          </header>
        )
      }

    }),

    renderType:'staticFromReact',
    elementType:'html',

    struct : {

    },

  };
