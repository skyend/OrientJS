var express = require('express');
var router = express.Router();

/* GET home page. */
router.post('/register', function(req, res, next) {
  console.log(agent.businessMan.registerUser);
});

export default router;